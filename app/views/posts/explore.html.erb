<div class="w-full max-w-5xl mx-auto text-gray-300">
  <% content_for :title, "Explore Posts" %>

  <div id="posts" class="space-y-6">
    <% if @posts.load.any? %>
      <%= render @posts, posts_liked_ids: @posts_liked_ids, class: "bg-primary-800 rounded-xl shadow-xl" %>
    <% else %>
      <div class="flex flex-col items-center justify-center py-20 text-center bg-primary-800 rounded-xl shadow-lg">
        <h2 class="text-2xl font-semibold text-secondary-400 mb-4" style="font-family: var(--font-heading)">
          Nothing to explore yet
        </h2>
        <p class="text-gray-400 max-w-md mb-6" style="font-family: var(--font-body)">
          It looks a bit quiet here. Be the first to post something and start the conversation.
        </p>
        <%= link_to "Create a Post", new_post_path, class: "px-5 py-2 rounded-full bg-accent-600 hover:bg-accent-500 text-primary-900 font-semibold transition duration-200" %>
      </div>
    <% end %>
  </div>
</div>
